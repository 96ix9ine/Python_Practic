repos:
  - repo: local
    hooks:
      - id: black
        name: black
        entry: black
        language: system
        types: [python]

      - id: mypy
        name: mypy
        entry: mypy
        language: system
        types: [python]
        args: [--explicit-package-bases, --ignore-missing-imports]

      - id: check-added-large-files
        name: Check for added large files
        entry: python -c "import sys, os; sys.exit(any(os.path.getsize(f) > 500*1024 for f in sys.argv[1:] if os.path.isfile(f) and (f.endswith('.py') or f.endswith('.env'))))"
        language: system
        types: [file]

      - id: detect-private-key
        name: Detect Private Key
        entry: python -c "import sys, os; sys.exit(any(('AWS_SECRET' in open(f, errors='ignore').read() or 'PRIVATE ' + 'KEY' in open(f, errors='ignore').read()) for f in sys.argv[1:] if os.path.isfile(f) and (f.endswith('.py') or f.endswith('.env'))))"
        language: system
        types: [text]
